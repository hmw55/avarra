# Avarra environment configuration
# Copy this file to .env for local development and replace placeholders as needed.

# Example:
# DATABASE_URL=postgresql://username:password@localhost:5432/avarra

# Local PostgreSQL development configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=avarra
DB_USERNAME=avarra
DB_PASSWORD=replace-with-local-password
